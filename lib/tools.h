#ifndef TOOLS_H
#define TOOLS_H

#include <string>
#include <vector>
#include <sstream>

using namespace std;

string lastname(string path, char splitchar);
string firstname(string path, char splitchar);

#endif